# =============================================================================
# IDFC GenAI Invoice Extractor - Environment Variables
# =============================================================================
# 
# NOTE: Most critical settings are now HARDCODED in the Python files for
#       reliable offline deployment. This file is optional and only needed
#       for cloud API credentials (OpenAI/Azure) during development.
#
# HARDCODED SETTINGS (no .env needed):
#   - DISABLE_MODEL_SOURCE_CHECK=True  (in ocr_engine.py, executable.py)
#   - PIN_MEMORY=False                  (in yolo_detector.py, vlm_extractor.py)
#   - YOLO_OFFLINE=True                 (in yolo_detector.py, executable.py)
#   - HF_HUB_OFFLINE=1                  (in vlm_extractor.py, executable.py)
#   - TRANSFORMERS_OFFLINE=1            (in vlm_extractor.py, executable.py)
#
# =============================================================================

# -----------------------------------------------------------------------------
# OpenAI Configuration (for VLM extraction with --vlm_provider openai)
# DEVELOPMENT ONLY - not for offline deployment
# -----------------------------------------------------------------------------
OPENAI_API_KEY=sk-your-openai-api-key-here

# -----------------------------------------------------------------------------
# Azure OpenAI Configuration (for VLM extraction with --vlm_provider azure)
# DEVELOPMENT ONLY - not for offline deployment
# -----------------------------------------------------------------------------
AZURE_OPENAI_ENDPOINT=https://your-resource-name.openai.azure.com
AZURE_OPENAI_API_KEY=your-azure-openai-api-key-here
AZURE_OPENAI_DEPLOYMENT=gpt-4o
AZURE_OPENAI_API_VERSION=2024-02-15-preview

# -----------------------------------------------------------------------------
# EasyOCR Configuration (NOW HARDCODED - this line is for reference only)
# -----------------------------------------------------------------------------
# Skip model source connectivity check (speeds up startup)
# DISABLE_MODEL_SOURCE_CHECK=True  # <-- NOW HARDCODED in code

# -----------------------------------------------------------------------------
# Optional: CUDA/GPU Configuration
# -----------------------------------------------------------------------------
# Force CPU mode (useful if GPU has issues)
# CUDA_VISIBLE_DEVICES=-1

# -----------------------------------------------------------------------------
# Optional: Logging
# -----------------------------------------------------------------------------
# Set log level (DEBUG, INFO, WARNING, ERROR)
# LOGURU_LEVEL=INFO

# =============================================================================
# Quick Start Examples
# =============================================================================
#
# For OFFLINE deployment (recommended), just run:
#   python executable.py invoice.png
#
# For development with Azure (requires internet):
#   $env:AZURE_OPENAI_ENDPOINT = "https://your-resource.openai.azure.com"
#   $env:AZURE_OPENAI_API_KEY = "your-key"
#   $env:AZURE_OPENAI_DEPLOYMENT = "gpt-4o"
#   python executable.py --input invoice.png --use_vlm --vlm_provider azure
#
# =============================================================================
